<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.resistor">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Resistor"
        android:usesCleartextTraffic="true">

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/colorfan" />

        <activity
            android:name="com.example.resistor.LoadApp"
            android:label="@string/app_name"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Resistor.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.resistor.LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Resistor.NoActionBar"/>

        <activity
            android:name="com.example.resistor.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Resistor.NoActionBar"/>
        <!-- registering the receiver -->

    </application>
</manifest>